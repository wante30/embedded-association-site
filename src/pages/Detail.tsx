import type { Project, Activity } from '../content';
import { Link } from '../lib/router';
import { ProjectArt, ActivityArt } from '../components/Art';
import { ExampleNote, JoinBanner } from '../components/UI';
import { Icon } from '../components/Icon';
export function ProjectDetail({ project: p }: { project: Project }) { return <><section className="detail-head"><div className="container"><Link to="/projects" className="back-link">← 返回项目展示</Link><span className="eyebrow">{p.kicker}</span><h1>{p.name}</h1><p>{p.summary}</p><div className="tag-row">{p.tags.map(t => <span key={t} className="tag">{t}</span>)}{p.example && <span className="tag">概念示例</span>}</div></div></section><section className="section"><div className="container detail-grid"><aside className="detail-visual"><ProjectArt kind={p.art} image={p.image} alt={p.name}/><p>{p.image ? (p.example ? '项目图片 · 展示示例' : '项目照片') : '设备概念示意图 · 非真实项目实拍'}</p><div className="detail-meta"><h2>项目档案</h2><div><span>方向</span><strong>{p.category}</strong></div><div><span>当前状态</span><strong>{p.status}</strong></div><div><span>负责人 / 团队</span><strong>待确认</strong></div></div></aside><div className="detail-content"><ExampleNote>本页为项目结构示例，未声明真实实验结果。</ExampleNote><section><span className="eyebrow">01 / CONTEXT</span><h2>从什么问题出发</h2><p>{p.background}</p></section><section><span className="eyebrow">02 / APPROACH</span><h2>希望实现什么</h2><p>{p.goal}</p></section><section><span className="eyebrow">03 / SYSTEM</span><h2>让每个环节协同运行</h2><div className="system-flow">{p.architecture.map((s,i) => <div key={s}><span><small>0{i+1}</small>{s}</span>{i<p.architecture.length-1 && <Icon name="arrow" size={16}/>}</div>)}</div></section><section><span className="eyebrow">04 / RECORD</span><h2>阶段成果与验证记录</h2><p>{p.result}</p></section></div></div></section><JoinBanner/></>; }
export function ActivityDetail({ activity: a }: { activity: Activity }) { return <><section className="detail-head"><div className="container"><Link to="/activities" className="back-link">← 返回活动记录</Link><span className="eyebrow">{a.category} / 展示示例</span><h1>{a.title}</h1><p>{a.summary}</p></div></section><section className="section"><article className="container article-content"><ActivityArt kind={a.art} image={a.image} alt={a.example ? `${a.title} · 场景示意，非协会实拍` : a.title} example={a.example}/><ExampleNote>本页为活动档案示例，时间、地点、照片与报道须核实后补充。</ExampleNote><h2>一次实践，可以从这里开始。</h2><p>{a.body}</p><h2>参考活动内容</h2><ol className="article-outline">{a.outline.map((x,i) => <li key={x}><span>0{i+1}</span>{x}</li>)}</ol><Link to="/activities" className="button button--outline">探索其他活动<Icon name="arrow" size={17}/></Link></article></section></>; }
export function NotFound() { return <section className="not-found container"><span>404</span><h1>这条线路，还没有接通。</h1><p>页面可能已移动，或者这个地址还没有对应的内容。</p><Link to="/" className="button button--primary">回到首页<Icon name="arrow" size={18}/></Link></section>; }
