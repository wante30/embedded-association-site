import { useState } from 'react';
import { activities } from '../content';
import { ActivityCard } from '../components/Cards';
import { FilterTabs } from '../components/UI';
export default function Activities() { const [category,setCategory] = useState('全部'); const list = activities.filter(a => category === '全部' || category === a.category); return <div className="page-dark collection-page"><section className="collection-header"><div className="container"><div className="page-heading"><div><span className="eyebrow">BUILD TOGETHER</span><h1>把真实发生过的活动，<br/><span>一件件记录下来。</span></h1><p>技术培训 · 纳新活动 · 竞赛训练</p></div><span className="page-index">02<span>ACTIVITIES</span></span></div><FilterTabs items={['全部','技术培训','纳新活动','竞赛训练']} value={category} onChange={setCategory}/></div></section><section className="collection-results"><div className="container"><p className="results-count" aria-live="polite">{list.length} 项活动记录</p><div className="activity-grid">{list.map(a => <ActivityCard key={a.id} activity={a}/>)}</div></div></section><div className="activity-quote container"><IconSquare/><blockquote>有些知识，只有亲手做过才真正属于你。</blockquote><span>LEARN BY BUILDING.</span></div></div>; }
function IconSquare() {return <span className="quote-mark">“</span>;}
