import { useState } from 'react';
import { awards } from '../content';
import { Icon } from '../components/Icon';
import { ExampleNote, FilterTabs, JoinBanner } from '../components/UI';
export default function Awards() { const [level,setLevel] = useState('全部'); const list = awards.filter(a => level === '全部' || a.level === level); const years = [...new Set(list.map(a => a.year))]; return <><section className="section awards-page"><div className="container"><div className="awards-heading"><div><span className="eyebrow">MILESTONES & RECOGNITION</span><h1>每一步积累，<br/><span className="blue-text">都值得被认真记录。</span></h1><p>以赛促学，以做促思。记录成果，也记录走过的路。</p></div><div className="award-emblem" aria-hidden="true"><div/><Icon name="trophy" size={78}/><span>EFFORT BECOMES EXPERIENCE</span></div></div><FilterTabs items={['全部','国家级','省级','校级']} value={level} onChange={setLevel}/><ExampleNote>以下为荣誉档案版式示例，年份、项目与奖项均需核实后替换。</ExampleNote><div className="awards-timeline">{years.map(year => <section className="timeline-year" key={year}><div className="year-label"><strong>{year}</strong><small>展示年度</small></div><div className="timeline-records">{list.filter(a => a.year === year).map(a => <article className="award-card" key={a.id}><span className="timeline-dot"/><div className="award-card__main"><div className="award-card__labels"><span className="tag tag--blue">{a.level} · 示例</span><span>成果资料待完善</span></div><h2>{a.name}</h2><p className="award-name">{a.award}</p><p>{a.project} · 团队信息待确认</p></div><div className="award-icon" aria-hidden="true"><Icon name={a.level === '国家级' ? 'trophy' : 'shield'} size={43}/></div></article>)}</div></section>)}</div></div></section><JoinBanner/></>; }
