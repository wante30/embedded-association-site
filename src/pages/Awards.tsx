import { useState } from 'react';
import { awards } from '../content';
import { Icon } from '../components/Icon';
import { FilterTabs, JoinBanner } from '../components/UI';
export default function Awards() { const [level,setLevel] = useState('全部'); const list = awards.filter(a => level === '全部' || a.level === level); const years = [...new Set(list.map(a => a.year))]; return <><section className="section awards-page"><div className="container"><div className="awards-heading"><div><span className="eyebrow">MILESTONES & RECOGNITION</span><h1>把比赛当成检验，<br/><span className="blue-text">也把每一次积累认真记录。</span></h1><p>以下为已确认可公开的协会成员与团队竞赛成果。</p></div><div className="award-emblem" aria-hidden="true"><div/><Icon name="trophy" size={78}/><span>EFFORT BECOMES EXPERIENCE</span></div></div><FilterTabs items={['全部','国家级','赛区','省级']} value={level} onChange={setLevel}/><div className="awards-timeline">{years.map(year => <section className="timeline-year" key={year}><div className="year-label"><strong>{year}</strong><small>竞赛记录</small></div><div className="timeline-records">{list.filter(a => a.year === year).map(a => <article className="award-card" key={a.id}><span className="timeline-dot"/><div className="award-card__main"><div className="award-card__labels"><span className="tag tag--blue">{a.level}</span><span>指导教师：{a.advisor}</span></div><h2>{a.name}</h2><p className="award-name">{a.award}</p><p>参赛成员：{a.team}</p></div><div className="award-icon" aria-hidden="true"><Icon name={a.level === '国家级' ? 'trophy' : 'shield'} size={43}/></div></article>)}</div></section>)}</div></div></section><JoinBanner/></>; }
