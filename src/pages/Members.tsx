import { useState } from 'react';
import { members } from '../content';
import { MemberCard } from '../components/Cards';
import { EmptyState, FilterTabs, JoinBanner } from '../components/UI';
export default function Members() { const [group,setGroup] = useState('全部'); const list = members.filter(m => group === '全部' || m.group === group); return <><section className="section members-page"><div className="container"><div className="member-heading"><div><span className="eyebrow">PEOPLE BEHIND THE ASSOCIATION</span><h1>一起做项目，<br/><span className="blue-text">也一起维护这个实践空间。</span></h1><p>当前先展示已经确认可公开的指导教师与学生管理员信息。</p></div><div className="people-decoration" aria-hidden="true"><span/><span/><span/><span/><i>BETTER TOGETHER.</i></div></div><FilterTabs items={['全部','指导教师','学生管理员']} value={group} onChange={setGroup}/>{list.length ? <div className="members-grid">{list.map(m => <MemberCard key={m.id} member={m}/>)}</div> : <EmptyState reset={() => setGroup('全部')}/>}</div></section><JoinBanner/></>; }
